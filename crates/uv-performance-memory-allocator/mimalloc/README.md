# mimalloc

This directory vendors mimalloc 3.3.2 from `libmimalloc-sys` 0.1.49.

The source is unmodified except for allowing the purge delay and arena purge
multiplier defaults in `src/options.c` to be set by the build.
