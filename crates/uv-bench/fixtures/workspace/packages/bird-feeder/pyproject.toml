[project]
name = "benchmark-bird-feeder"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "packaging==24.1",
    "benchmark-seeds",
]

[tool.uv.sources]
benchmark-seeds = { workspace = true }
