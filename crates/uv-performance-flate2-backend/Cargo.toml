[package]
name = "uv-performance-flate2-backend"
version = "0.1.0"
publish = false
edition = "2021"

[lib]
doctest = false

[dependencies]
flate2 = { version = "1.0.28", default-features = false, features = ["zlib-rs"] }
