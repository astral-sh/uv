[project]
name = "bird-feeder"
version = "1.0.0"
dependencies = ["anyio>=4.3.0,<5", "seeds", "a"]

[tool.uv.sources]
seeds = { workspace = true }
a = { path = "../../../ab", editable = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
