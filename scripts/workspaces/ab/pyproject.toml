[project]
name = "a"
version = "0.1.0"
dependencies = ["b"]

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
b = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
