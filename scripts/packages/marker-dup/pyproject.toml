[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "marker-dup"
version = "0.1.0"
description = "Package with duplicated marker-specific dependencies"
dependencies = [
  "dup-target>=0.3.6; python_version >= '3.11'",
  "dup-target>=0.4.0; python_version >= '3.12'",
]
