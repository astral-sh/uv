[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bz2"]

[tool.hatch.build.targets.sdist]
only-include = ["src/bz2"]

[project]
name = "bz2"
version = "1.0.0"
dependencies = []
requires-python = ">=3.8"
description = ""

[project.optional-dependencies]
