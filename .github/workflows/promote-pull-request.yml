name: Promote pull request

on:
  workflow_dispatch:
    inputs:
      pull_request:
        description: "The uv-dev pull request number"
        required: true
        type: string
      head_sha:
        description: "The uv-dev pull request head SHA approved for promotion"
        required: true
        type: string

permissions: {}

env:
  UV_REPOSITORY_ID: "699532645"
  UV_DEV_REPOSITORY_ID: "1302176231"
  AUTOMATIONS_BOT_ID: "305554984"

concurrency:
  group: promote-pull-request-${{ inputs.pull_request }}
  cancel-in-progress: false

jobs:
  promote:
    name: "Promote pull request #${{ inputs.pull_request }}"
    if: github.repository == 'astral-sh/uv-dev'
    runs-on: ubuntu-slim
    environment: automations
    timeout-minutes: 10
    permissions:
      id-token: write # for the credential broker
      pull-requests: read # to verify the uv-dev pull request
    steps:
      - name: Verify pull request
        id: verify
        env:
          EXPECTED_HEAD_SHA: ${{ inputs.head_sha }}
          GH_TOKEN: ${{ github.token }}
          PULL_REQUEST_NUMBER: ${{ inputs.pull_request }}
        run: |
          if [[ ! "$EXPECTED_HEAD_SHA" =~ ^[0-9a-f]{40}$ ]]; then
            echo "An approved pull request head SHA is required; refusing to promote it." >&2
            exit 1
          fi

          gh pr view "$PULL_REQUEST_NUMBER" \
            --repo "$GITHUB_REPOSITORY" \
            --json number,title,body,state,isDraft,baseRefName,headRefName,headRefOid,isCrossRepository \
            > "$RUNNER_TEMP/pull-request.json"

          if ! jq --exit-status \
            '.state == "OPEN" and (.isDraft | not) and (.isCrossRepository | not)' \
            "$RUNNER_TEMP/pull-request.json" > /dev/null; then
            echo "The uv-dev pull request is no longer ready; refusing to promote it." >&2
            exit 1
          fi

          head_ref=$(jq --raw-output '.headRefName' "$RUNNER_TEMP/pull-request.json")
          base_ref=$(jq --raw-output '.baseRefName' "$RUNNER_TEMP/pull-request.json")
          git check-ref-format "refs/heads/$head_ref"
          git check-ref-format "refs/heads/$base_ref"

          events=$(gh api --method GET "repos/$GITHUB_REPOSITORY/issues/$PULL_REQUEST_NUMBER/events" --paginate --slurp)
          latest_ready=$(jq --compact-output '[.[][] | select(.event == "ready_for_review")] | last // null' <<< "$events")
          if ! jq --exit-status '.actor.type == "User"' <<< "$latest_ready" > /dev/null; then
            if ! jq --exit-status --argjson bot_id "$AUTOMATIONS_BOT_ID" '.actor.id == $bot_id' <<< "$latest_ready" > /dev/null; then
              echo "The pull request was not marked ready by a human or the rebase automation." >&2
              exit 1
            fi
            approval_id=$(jq --raw-output '[.[][] | select(.event == "ready_for_review" and .actor.type == "User") | .id | tostring] | last // empty' <<< "$events")
            comments=$(gh api --method GET "repos/$GITHUB_REPOSITORY/issues/$PULL_REQUEST_NUMBER/comments" --paginate --slurp)
            if ! jq --exit-status \
              --argjson bot_id "$AUTOMATIONS_BOT_ID" --arg approval_id "$approval_id" \
              --arg head_ref "$head_ref" --arg head_sha "$EXPECTED_HEAD_SHA" --arg base_ref "$base_ref" '
                def record($marker):
                  .body | split("<!-- " + $marker + ":")[1]? | select(type == "string") | split(" -->")[0] | fromjson?;
                [.[][] | select(.user.id == $bot_id and .performed_via_github_app.slug == "astral-automations-bot")] as $comments |
                any($comments[] | record("uv-promotion-clean-rebase");
                  . as $receipt |
                  $approval_id != "" and .approval_id == $approval_id and .rebased_head_sha == $head_sha and .head_ref == $head_ref and .base_ref == $base_ref and
                  any($comments[];
                    (.id | tostring) == $receipt.request_id and record("uv-promotion-rebase-request") == ($receipt | del(.request_id, .rebased_head_sha, .rebased_base_sha))))
              ' <<< "$comments" > /dev/null; then
              echo "The bot-marked revision has no matching clean rebase approval; refusing to promote it." >&2
              exit 1
            fi
          fi

          {
            echo "base_ref=$base_ref"
            echo "head_ref=$head_ref"
          } >> "$GITHUB_OUTPUT"

      - name: Get promotion token
        id: token
        uses: open-security-tools/ost-simple-sts@974a63a2daaaa40f7c6dec40d334f3da4421469e
        with:
          exchange-url: ${{ secrets.STS_API_URL }}/exchange
          audience: ${{ secrets.STS_API_URL }}
          repositories: |
            astral-sh/uv
            astral-sh/uv-dev
          # `workflows: write` is required when the promoted branch modifies workflow files.
          permissions: |
            contents: write
            pull_requests: write
            workflows: write

      - name: Promote pull request
        id: promote
        env:
          BASE_REF: ${{ steps.verify.outputs.base_ref }}
          EXPECTED_HEAD_SHA: ${{ inputs.head_sha }}
          GH_TOKEN: ${{ steps.token.outputs.token }}
          HEAD_REF: ${{ steps.verify.outputs.head_ref }}
          PULL_REQUEST_NUMBER: ${{ inputs.pull_request }}
        run: |
          reject() {
            echo "rejection_reason=$1" >> "$GITHUB_OUTPUT"
            echo "$1" >&2
            exit 1
          }

          source_pull_request=$(gh api --method GET "repos/astral-sh/uv-dev/pulls/$PULL_REQUEST_NUMBER")
          approval=$(
            gh api \
              --method GET \
              "repos/$GITHUB_REPOSITORY/issues/$PULL_REQUEST_NUMBER/events" \
              --paginate \
              --slurp |
              jq --compact-output '[.[][] | select(.event == "ready_for_review" and .actor.type == "User")] | last // null'
          )
          promoter=$(jq --raw-output '.actor.login // empty' <<< "$approval")
          if [ -z "$promoter" ]; then
            echo "The pull request does not have a human promoter; refusing to promote it." >&2
            exit 1
          fi
          echo "promoter=$promoter" >> "$GITHUB_OUTPUT"
          echo "approval_id=$(jq --raw-output '.id' <<< "$approval")" >> "$GITHUB_OUTPUT"

          if ! jq --exit-status \
            --arg base_ref "$BASE_REF" \
            --arg head_ref "$HEAD_REF" \
            --argjson repository_id "$UV_DEV_REPOSITORY_ID" \
            '.state == "open" and (.draft | not) and .base.ref == $base_ref and .head.ref == $head_ref and .base.repo.id == $repository_id and .head.repo.id == $repository_id' \
            <<< "$source_pull_request" > /dev/null; then
            reject "The uv-dev pull request is no longer ready on the expected branch."
          fi

          source_head=$(jq --raw-output '.head.sha' <<< "$source_pull_request")
          if [ "$source_head" != "$EXPECTED_HEAD_SHA" ]; then
            echo "changed_head=$source_head" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          base_head=$(jq --raw-output '.base.sha' <<< "$source_pull_request")

          upstream_base_refs=$(gh api --method GET "repos/astral-sh/uv/git/matching-refs/heads/$BASE_REF")
          if ! jq --exit-status --arg ref "refs/heads/$BASE_REF" 'any(.[]; .ref == $ref)' <<< "$upstream_base_refs" > /dev/null; then
            parent_pull_requests=$(
              gh pr list \
                --repo astral-sh/uv \
                --state open \
                --head "$BASE_REF" \
              --limit 2 \
              --json headRefName,headRefOid,headRepository
            )

            if [ "$(jq 'length' <<< "$parent_pull_requests")" = "0" ]; then
              source_parent_pull_requests=$(
                gh pr list \
                  --repo "$GITHUB_REPOSITORY" \
                  --state open \
                  --head "$BASE_REF" \
                  --limit 2 \
                  --json number,headRefName,headRefOid,headRepository
              )
              if [ "$(jq 'length' <<< "$source_parent_pull_requests")" != "0" ]; then
                if ! jq --exit-status \
                  --arg base_ref "$BASE_REF" \
                  --arg base_head "$base_head" \
                  'length == 1 and .[0].headRefName == $base_ref and .[0].headRefOid == $base_head and .[0].headRepository.nameWithOwner == "astral-sh/uv-dev"' \
                  <<< "$source_parent_pull_requests" > /dev/null; then
                  reject "The upstream base branch $BASE_REF does not have a matching open parent pull request."
                fi

                parent_number=$(jq --raw-output '.[0].number' <<< "$source_parent_pull_requests")
                gh pr comment "$PULL_REQUEST_NUMBER" \
                  --repo "$GITHUB_REPOSITORY" \
                  --body "Waiting for promotion of parent #$parent_number"
                echo "queued=true" >> "$GITHUB_OUTPUT"
                exit 0
              fi

              merged_parent_pull_requests=$(
                gh pr list \
                  --repo astral-sh/uv \
                  --state merged \
                  --head "$BASE_REF" \
                  --limit 2 \
                  --json number,baseRefName,headRefName,headRefOid,headRepository
              )
              if ! jq --exit-status \
                --arg base_ref "$BASE_REF" \
                'length == 1 and .[0].headRefName == $base_ref and .[0].headRepository.nameWithOwner == "astral-sh/uv"' \
                <<< "$merged_parent_pull_requests" > /dev/null; then
                reject "The upstream base branch $BASE_REF does not have a matching open or merged parent pull request."
              fi

              parent_number=$(jq --raw-output '.[0].number' <<< "$merged_parent_pull_requests")
              merged_parent=$(gh api --method GET "repos/astral-sh/uv/pulls/$parent_number")
              if ! jq --exit-status \
                --arg base_ref "$BASE_REF" \
                --argjson repository_id "$UV_REPOSITORY_ID" \
                '.merged_at != null and .head.ref == $base_ref and .base.repo.id == $repository_id and .head.repo.id == $repository_id' \
                <<< "$merged_parent" > /dev/null; then
                reject "The merged upstream parent for $BASE_REF changed before it could be verified."
              fi

              parent_head=$(jq --raw-output '.head.sha' <<< "$merged_parent")
              if [ "$parent_head" != "$base_head" ]; then
                # The copied upstream branch may have been rebased after promotion. Only
                # accept that history when our bot recorded the exact source promotion.
                closed_source_parents=$(
                  gh pr list --repo "$GITHUB_REPOSITORY" --state closed --head "$BASE_REF" \
                    --limit 2 --json number,headRefName,headRefOid,headRepository
                )
                if ! jq --exit-status \
                  --arg base_ref "$BASE_REF" --arg base_head "$base_head" \
                  'length == 1 and .[0].headRefName == $base_ref and .[0].headRefOid == $base_head and .[0].headRepository.nameWithOwner == "astral-sh/uv-dev"' \
                  <<< "$closed_source_parents" > /dev/null; then
                  reject "The merged parent of $BASE_REF is at $parent_head, but the original source parent at $base_head could not be verified."
                fi
                source_parent_number=$(jq --raw-output '.[0].number' <<< "$closed_source_parents")
                source_parent=$(gh api --method GET "repos/$GITHUB_REPOSITORY/pulls/$source_parent_number")
                if ! jq --exit-status \
                  --arg base_ref "$BASE_REF" --arg base_head "$base_head" \
                  --argjson repository_id "$UV_DEV_REPOSITORY_ID" \
                  '.state == "closed" and .head.ref == $base_ref and .head.sha == $base_head and .base.repo.id == $repository_id and .head.repo.id == $repository_id' \
                  <<< "$source_parent" > /dev/null; then
                  reject "The original source parent of $BASE_REF changed before it could be verified."
                fi
                promotion_comment="Promoted to [#$parent_number](https://github.com/astral-sh/uv/pull/$parent_number)."
                parent_comments=$(gh api --method GET "repos/$GITHUB_REPOSITORY/issues/$source_parent_number/comments" --paginate --slurp)
                if ! jq --exit-status \
                  --arg body "$promotion_comment" --argjson bot_id "$AUTOMATIONS_BOT_ID" \
                  'any(.[][]; .user.id == $bot_id and .performed_via_github_app.slug == "astral-automations-bot" and .body == $body)' \
                  <<< "$parent_comments" > /dev/null \
                  || ! jq --exit-status --argjson bot_id "$AUTOMATIONS_BOT_ID" '.user.id == $bot_id' <<< "$merged_parent" > /dev/null; then
                  reject "The merged parent of $BASE_REF is at $parent_head, but its promotion from $base_head could not be verified."
                fi

                parent_history_query=$(cat <<'GRAPHQL'
          query($number: Int!, $endCursor: String) {
            repository(owner: "astral-sh", name: "uv") {
              pullRequest(number: $number) {
                timelineItems(first: 100, after: $endCursor, itemTypes: [HEAD_REF_FORCE_PUSHED_EVENT]) {
                  nodes {
                    ... on HeadRefForcePushedEvent {
                      beforeCommit { oid }
                      afterCommit { oid }
                    }
                  }
                  pageInfo { hasNextPage endCursor }
                }
              }
            }
          }
          GRAPHQL
                )
                parent_history=$(
                  gh api graphql --paginate --slurp --field number="$parent_number" --raw-field query="$parent_history_query"
                )
                if ! jq --exit-status --arg base_head "$base_head" \
                  'any(.[].data.repository.pullRequest.timelineItems.nodes[]; .beforeCommit.oid == $base_head or .afterCommit.oid == $base_head)' \
                  <<< "$parent_history" > /dev/null; then
                  reject "The original parent revision $base_head is not recorded in the upstream history of astral-sh/uv#$parent_number."
                fi
              fi

              parent_base_ref=$(jq --raw-output '.base.ref' <<< "$merged_parent")
              parent_merge_sha=$(jq --raw-output '.merge_commit_sha' <<< "$merged_parent")
              git check-ref-format "refs/heads/$parent_base_ref"
              parent_base_sha=$(gh api --method GET "repos/$GITHUB_REPOSITORY/git/ref/heads/$parent_base_ref" --jq .object.sha)
              comparison=$(gh api --method GET "repos/$GITHUB_REPOSITORY/compare/$parent_merge_sha...$parent_base_sha")
              if ! jq --exit-status '.status | IN("ahead", "identical")' <<< "$comparison" > /dev/null; then
                gh pr comment "$PULL_REQUEST_NUMBER" --repo "$GITHUB_REPOSITORY" \
                  --body "Waiting for astral-sh/uv#$parent_number to sync to $parent_base_ref."
                echo "queued=true" >> "$GITHUB_OUTPUT"
                exit 0
              fi
              current_pull_request=$(gh api --method GET "repos/$GITHUB_REPOSITORY/pulls/$PULL_REQUEST_NUMBER")
              if ! jq --exit-status \
                --arg base_ref "$BASE_REF" \
                --arg base_head "$base_head" \
                --arg head_ref "$HEAD_REF" \
                --arg head_sha "$source_head" \
                --argjson repository_id "$UV_DEV_REPOSITORY_ID" \
                '.state == "open" and (.draft | not) and .base.ref == $base_ref and .base.sha == $base_head and .head.ref == $head_ref and .head.sha == $head_sha and .base.repo.id == $repository_id and .head.repo.id == $repository_id' \
                <<< "$current_pull_request" > /dev/null; then
                reject "The pull request changed before its merged parent could be retargeted."
              fi

              {
                echo "rejection_reason=The parent was merged as astral-sh/uv#$parent_number; the child must be rebased onto its merged parent."
                echo "rebase_ref=$parent_base_ref"
                echo "rebase_base_sha=$parent_base_sha"
                echo "base_previous_sha=$base_head"
              } >> "$GITHUB_OUTPUT"
              exit 0
            fi

            if ! jq --exit-status \
              --arg base_ref "$BASE_REF" \
              --arg base_head "$base_head" \
              'length == 1 and .[0].headRefName == $base_ref and .[0].headRefOid == $base_head and .[0].headRepository.nameWithOwner == "astral-sh/uv-dev"' \
              <<< "$parent_pull_requests" > /dev/null; then
              reject "The upstream base branch $BASE_REF does not have a matching open parent pull request."
            fi

            gh api \
              --method POST \
              repos/astral-sh/uv/git/refs \
              --raw-field ref="refs/heads/$BASE_REF" \
              --raw-field sha="$base_head" \
              --silent
          fi

          existing_pull_requests=$(
            gh api \
              --method GET \
              repos/astral-sh/uv/pulls \
              --raw-field state=all \
              --raw-field head="astral-sh:$HEAD_REF" |
              jq --compact-output \
                --argjson repository_id "$UV_REPOSITORY_ID" \
                '[.[] | select(.head.repo.id == $repository_id)]'
          )
          existing_count=$(jq 'length' <<< "$existing_pull_requests")
          if (( existing_count > 1 )); then
            reject "Found multiple upstream pull requests for $HEAD_REF."
          fi

          if (( existing_count == 1 )); then
            if ! jq --exit-status \
              --arg base_ref "$BASE_REF" \
              --arg head_ref "$HEAD_REF" \
              --arg head_sha "$source_head" \
              --argjson repository_id "$UV_REPOSITORY_ID" \
              '.[0].state == "open" and .[0].base.ref == $base_ref and .[0].head.ref == $head_ref and .[0].head.sha == $head_sha and .[0].base.repo.id == $repository_id and .[0].head.repo.id == $repository_id' \
              <<< "$existing_pull_requests" > /dev/null; then
              reject "An upstream pull request already exists with a different state, base, or head."
            fi

            upstream_pull_request=$(jq --compact-output '.[0]' <<< "$existing_pull_requests")
          else
            upstream_refs=$(gh api --method GET "repos/astral-sh/uv/git/matching-refs/heads/$HEAD_REF")
            upstream_head=$(
              jq --raw-output \
                --arg head_ref "refs/heads/$HEAD_REF" \
                '.[] | select(.ref == $head_ref) | .object.sha' \
                <<< "$upstream_refs"
            )

            if [ -n "$upstream_head" ] && [ "$upstream_head" != "$source_head" ]; then
              reject "The upstream branch $HEAD_REF already exists at $upstream_head instead of $source_head."
            fi

            if [ -z "$upstream_head" ]; then
              gh api \
                --method POST \
                repos/astral-sh/uv/git/refs \
                --raw-field ref="refs/heads/$HEAD_REF" \
                --raw-field sha="$source_head" \
                --silent
            fi

            upstream_pull_request=$(
              jq \
                --arg base_ref "$BASE_REF" \
                --arg head_ref "$HEAD_REF" \
                --arg source_repository "$GITHUB_REPOSITORY" \
                '{
                  title: .title,
                  body: (
                    (.body // "")
                    | gsub(
                      "(?<![[:alnum:]_/])#(?<number>[0-9]+)(?![[:alnum:]_])";
                      "\($source_repository)#\(.number)"
                    )
                  ),
                  base: $base_ref,
                  head: $head_ref
                }' \
                "$RUNNER_TEMP/pull-request.json" |
                gh api \
                  --method POST \
                  repos/astral-sh/uv/pulls \
                  --input -
            )
          fi

          upstream_number=$(jq --exit-status --raw-output '.number | select(type == "number")' <<< "$upstream_pull_request")

          if jq --exit-status '.labels | length > 0' <<< "$source_pull_request" > /dev/null; then
            jq '{labels: [.labels[].name]}' <<< "$source_pull_request" |
              gh api \
                --method POST \
                "repos/astral-sh/uv/issues/$upstream_number/labels" \
                --input - \
                --silent
          fi

          gh api \
            --method POST \
            "repos/astral-sh/uv/issues/$upstream_number/assignees" \
            --raw-field "assignees[]=$promoter" \
            --silent

          echo "number=$upstream_number" >> "$GITHUB_OUTPUT"

      - name: Close uv-dev pull request
        id: close
        if: steps.promote.outputs.number != ''
        env:
          EXPECTED_HEAD_SHA: ${{ inputs.head_sha }}
          GH_TOKEN: ${{ steps.token.outputs.token }}
          PULL_REQUEST_NUMBER: ${{ inputs.pull_request }}
          UPSTREAM_NUMBER: ${{ steps.promote.outputs.number }}
        run: |
          pull_request=$(gh pr view "$PULL_REQUEST_NUMBER" --repo "$GITHUB_REPOSITORY" --json state,isDraft,headRefOid)
          if ! jq --exit-status \
            '.state == "OPEN" and (.isDraft | not)' \
            <<< "$pull_request" > /dev/null; then
            echo "The uv-dev pull request is no longer ready; refusing to close it." >&2
            exit 1
          fi

          upstream_url="https://github.com/astral-sh/uv/pull/$UPSTREAM_NUMBER"
          source_head=$(jq --raw-output '.headRefOid' <<< "$pull_request")
          promoted_head=$(gh pr view "$UPSTREAM_NUMBER" --repo astral-sh/uv --json headRefOid --jq .headRefOid)
          if [ "$source_head" != "$EXPECTED_HEAD_SHA" ]; then
            echo "changed_head=$source_head" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if [ "$promoted_head" != "$EXPECTED_HEAD_SHA" ]; then
            echo "changed_head=$promoted_head" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          gh pr close "$PULL_REQUEST_NUMBER" \
            --repo "$GITHUB_REPOSITORY" \
            --comment "Promoted to [#$UPSTREAM_NUMBER]($upstream_url)."

          echo "Promoted uv-dev pull request #$PULL_REQUEST_NUMBER to [#$UPSTREAM_NUMBER]($upstream_url)." >> "$GITHUB_STEP_SUMMARY"

      - name: Recover rejected promotion
        if: ${{ always() && steps.token.outcome == 'success' && (steps.promote.outputs.rejection_reason != '' || steps.promote.outputs.changed_head != '' || steps.close.outputs.changed_head != '') }}
        env:
          APPROVAL_ID: ${{ steps.promote.outputs.approval_id }}
          BASE_REF: ${{ steps.verify.outputs.base_ref }}
          BASE_PREVIOUS_SHA: ${{ steps.promote.outputs.base_previous_sha }}
          CHANGED_HEAD: ${{ steps.promote.outputs.changed_head || steps.close.outputs.changed_head }}
          EXPECTED_HEAD_SHA: ${{ inputs.head_sha }}
          GH_TOKEN: ${{ steps.token.outputs.token }}
          HEAD_REF: ${{ steps.verify.outputs.head_ref }}
          PROMOTER: ${{ steps.promote.outputs.promoter }}
          PULL_REQUEST_NUMBER: ${{ inputs.pull_request }}
          REBASE_BASE_SHA: ${{ steps.promote.outputs.rebase_base_sha }}
          REBASE_REF: ${{ steps.promote.outputs.rebase_ref }}
          REJECTION_REASON: ${{ steps.promote.outputs.rejection_reason }}
          UPSTREAM_NUMBER: ${{ steps.promote.outputs.number }}
        run: |
          current_approval=$(
            gh api --method GET "repos/$GITHUB_REPOSITORY/issues/$PULL_REQUEST_NUMBER/events" --paginate --slurp |
              jq --raw-output '[.[][] | select(.event == "ready_for_review" and .actor.type == "User") | .id] | last // empty'
          )
          pull_request=$(gh api --method GET "repos/$GITHUB_REPOSITORY/pulls/$PULL_REQUEST_NUMBER")
          if [ -z "$APPROVAL_ID" ] || [ "$current_approval" != "$APPROVAL_ID" ] || ! jq --exit-status \
            --argjson repository_id "$UV_DEV_REPOSITORY_ID" \
            '.state == "open" and .base.repo.id == $repository_id and .head.repo.id == $repository_id' \
            <<< "$pull_request" > /dev/null; then
            echo "The rejected promotion was superseded; leaving the pull request unchanged."
            exit 0
          fi

          if [ -n "$REBASE_REF" ] && ! jq --exit-status \
            --arg base_ref "$BASE_REF" --arg base_sha "$BASE_PREVIOUS_SHA" \
            --arg head_ref "$HEAD_REF" --arg head_sha "$EXPECTED_HEAD_SHA" \
            '.base.ref == $base_ref and .base.sha == $base_sha and .head.ref == $head_ref and .head.sha == $head_sha' \
            <<< "$pull_request" > /dev/null; then
            echo "The stacked pull request changed before recovery; leaving it unchanged."
            exit 0
          fi

          if jq --exit-status '.draft | not' <<< "$pull_request" > /dev/null; then
            gh pr ready "$PULL_REQUEST_NUMBER" --repo "$GITHUB_REPOSITORY" --undo
          fi
          gh api \
            --method POST \
            "repos/$GITHUB_REPOSITORY/issues/$PULL_REQUEST_NUMBER/assignees" \
            --raw-field "assignees[]=$PROMOTER" \
            --silent

          if [ -z "$REJECTION_REASON" ]; then
            REJECTION_REASON="Commit $CHANGED_HEAD does not match the approved commit $EXPECTED_HEAD_SHA."
          fi
          comment="Promotion stopped: $REJECTION_REASON Returned this pull request to draft and assigned @$PROMOTER."
          if [ -n "$REBASE_REF" ]; then
            recovery=$(jq --compact-output --null-input \
              --arg approval_id "$APPROVAL_ID" --arg head_ref "$HEAD_REF" --arg head_sha "$EXPECTED_HEAD_SHA" \
              --arg base_previous_sha "$BASE_PREVIOUS_SHA" --arg base_ref "$REBASE_REF" --arg base_sha "$REBASE_BASE_SHA" \
              '{approval_id: $approval_id, head_ref: $head_ref, head_sha: $head_sha, base_previous_sha: $base_previous_sha, base_ref: $base_ref, base_sha: $base_sha}')
            printf -v comment "%s Retargeting to \`%s\` will rebase only the commits after \`%s\`. A clean Git rebase can resume promotion under the original approval. If resolving conflicts requires edits, review the result and mark it ready again. <!-- uv-promotion-rebase-request:%s -->" \
              "$comment" "$REBASE_REF" "$BASE_PREVIOUS_SHA" "$recovery"
          else
            comment="$comment Please review the updated changes and mark it ready for review again."
          fi
          if [ -n "$UPSTREAM_NUMBER" ]; then
            comment="$comment An upstream pull request was already created at [#$UPSTREAM_NUMBER](https://github.com/astral-sh/uv/pull/$UPSTREAM_NUMBER)."
          fi
          marker="<!-- uv-promotion-rejection:$APPROVAL_ID:$EXPECTED_HEAD_SHA -->"
          comment="$comment [Promotion run](https://github.com/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID). $marker"
          comments=$(gh api --method GET "repos/$GITHUB_REPOSITORY/issues/$PULL_REQUEST_NUMBER/comments" --paginate --slurp)
          comment_id=$(jq --raw-output --arg marker "$marker" --argjson bot_id "$AUTOMATIONS_BOT_ID" \
            '[.[][] | select(.user.id == $bot_id and (.body | contains($marker))) | .id] | last // empty' <<< "$comments")
          if [ -n "$comment_id" ]; then
            gh api --method PATCH "repos/$GITHUB_REPOSITORY/issues/comments/$comment_id" \
              --raw-field body="$comment" --silent
          else
            gh pr comment "$PULL_REQUEST_NUMBER" --repo "$GITHUB_REPOSITORY" --body "$comment"
          fi

          if [ -n "$REBASE_REF" ]; then
            # Publish the approval record before the base-change event starts the rebase.
            pull_request=$(gh api --method GET "repos/$GITHUB_REPOSITORY/pulls/$PULL_REQUEST_NUMBER")
            if ! jq --exit-status \
              --arg base_ref "$BASE_REF" --arg base_sha "$BASE_PREVIOUS_SHA" \
              --arg head_ref "$HEAD_REF" --arg head_sha "$EXPECTED_HEAD_SHA" \
              '.state == "open" and .draft and .base.ref == $base_ref and .base.sha == $base_sha and .head.ref == $head_ref and .head.sha == $head_sha' \
              <<< "$pull_request" > /dev/null; then
              echo "The stacked pull request changed before retargeting; refusing to update it." >&2
              exit 1
            fi
            gh api --method PATCH "repos/$GITHUB_REPOSITORY/pulls/$PULL_REQUEST_NUMBER" \
              --raw-field base="$REBASE_REF" --silent
          fi

          echo "Returned uv-dev pull request #$PULL_REQUEST_NUMBER to draft: $REJECTION_REASON" >> "$GITHUB_STEP_SUMMARY"
