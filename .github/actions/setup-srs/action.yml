name: "Setup srs"
description: "Install srs and restore its Cargo build cache"

inputs:
  artifact-cache-scope:
    description: "Stable name for the workload that reads and writes this artifact cache"
    required: true
  target-path:
    description: "Cargo target profile directory to cache"
    required: false
    default: ""

outputs:
  artifact-cache-hit:
    description: "Whether the primary srs artifact cache key was restored"
    value: ${{ steps.artifact-cache.outputs.cache-hit }}
  artifact-cache-primary-key:
    description: "Primary srs artifact cache key to use when saving"
    value: ${{ steps.artifact-cache.outputs.cache-primary-key }}
  target-cache-hit:
    description: "Whether the primary Cargo target cache key was restored"
    value: ${{ steps.target-cache.outputs.cache-hit }}
  target-cache-primary-key:
    description: "Primary Cargo target cache key to use when saving"
    value: ${{ steps.target-cache.outputs.cache-primary-key }}

runs:
  using: "composite"
  steps:
    - name: "Restore srs artifact cache"
      id: artifact-cache
      uses: actions/cache/restore@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
      with:
        path: |
          ~/.cargo/registry/index
          ~/.cargo/registry/cache
          ~/.cargo/git/db
          ~/.cargo/srs-artifact-cache-v2
        key: srs-artifacts-v2-2026.06.24-${{ runner.os }}-${{ runner.arch }}-${{ inputs.artifact-cache-scope }}-${{ hashFiles('**/Cargo.lock', '**/Cargo.toml', '**/rust-toolchain.toml', '.cargo/**') }}-${{ github.sha }}
        restore-keys: |
          srs-artifacts-v2-2026.06.24-${{ runner.os }}-${{ runner.arch }}-${{ inputs.artifact-cache-scope }}-${{ hashFiles('**/Cargo.lock', '**/Cargo.toml', '**/rust-toolchain.toml', '.cargo/**') }}-
          srs-artifacts-v2-2026.06.24-${{ runner.os }}-${{ runner.arch }}-${{ inputs.artifact-cache-scope }}-

    - name: "Restore Cargo target cache"
      if: ${{ inputs.target-path != '' }}
      id: target-cache
      uses: actions/cache/restore@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
      with:
        path: ${{ inputs.target-path }}
        key: srs-target-v2-2026.06.24-${{ runner.os }}-${{ runner.arch }}-${{ inputs.artifact-cache-scope }}-${{ github.job }}-${{ hashFiles('**/Cargo.lock', '**/Cargo.toml', '**/rust-toolchain.toml', '.cargo/**') }}-${{ github.sha }}
        restore-keys: |
          srs-target-v2-2026.06.24-${{ runner.os }}-${{ runner.arch }}-${{ inputs.artifact-cache-scope }}-${{ github.job }}-${{ hashFiles('**/Cargo.lock', '**/Cargo.toml', '**/rust-toolchain.toml', '.cargo/**') }}-
          srs-target-v2-2026.06.24-${{ runner.os }}-${{ runner.arch }}-${{ inputs.artifact-cache-scope }}-${{ github.job }}-

    - name: "Install srs"
      shell: bash
      run: bash "$GITHUB_ACTION_PATH/install.sh"
