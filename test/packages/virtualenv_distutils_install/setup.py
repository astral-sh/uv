from setuptools import setup


setup(
    name="virtualenv-distutils-install",
    version="0.0.0",
    py_modules=["virtualenv_distutils_install"],
    scripts=["scripts/virtualenv-distutils-install"],
)
