[project]
name = "uv-test-man-pages"
version = "0.1.0"
description = "A test package that installs a manual page"
readme = "README.md"
requires-python = ">=3.8"

[tool.uv.build-backend.data]
data = "data"

[build-system]
requires = ["uv_build>=0.8.0,<0.13"]
build-backend = "uv_build"
