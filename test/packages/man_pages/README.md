# uv-test-man-pages

A dependency-free Python package for testing wheel data installation. Its wheel installs
`uv-test-man-pages.1` into `man/man1` under the interpreter's data directory, exercising layouts
where `man` is a symlink to `share/man`.
