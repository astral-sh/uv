name = "prerelease-redundant-authorization-plain-first"
description = "Root requirements retain the stable preference when a narrower plain requirement appears first."

[root]
requires = ["c>=1.0", "c>=0.5a1"]

[expected]
satisfiable = true
explanation = "Requirements in the same root dependency batch retain the stable preference regardless of requirement order."

[expected.packages]
c = "1.0.0"

[packages.c.versions."1.0.0"]

[packages.c.versions."2.0.0a1"]
