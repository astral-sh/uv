name = "transitive-prerelease-equivalent-authorization-explicit-first"
description = "Requirements from one parent combine pre-release admission when the explicit requirement appears first."

[root]
requires = ["a"]

[expected]
satisfiable = true
explanation = "Requirements from one selected parent are combined before resolution, so the requirement naming a pre-release authorizes matching pre-releases."

[expected.packages]
a = "1.0.0"
c = "2.0.0a1"

[packages.a.versions."1.0.0"]
requires = ["c>=1.0,>0.5a1", "c>=1.0"]

[packages.c.versions."1.0.0"]

[packages.c.versions."2.0.0a1"]
