interface:
  display_name: "Load GitHub Action Thread"
  short_description: "Download and load a Codex Action thread"
  default_prompt: "Use $load-github-action-thread to find and load the Codex GitHub Action threads for a run or issue, then return links to the imported tasks."
