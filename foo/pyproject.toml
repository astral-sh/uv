# # Variation: it's requested as editable, but the project itself isn't a package.
# # So we build with the "wrong" request.

# [project]
# name = "foo"
# version = "0.1.0"
# description = "Add your description here"
# readme = "README.md"
# requires-python = ">=3.13.0"
# dependencies = [
#     "baz",
# ]

# [tool.uv]
# override-dependencies = ["baz"]

# [tool.uv.sources]
# baz = { path = ".", editable = true }

# Variation: it's requested as editable, and the project itself is a package.
# So they share a cache entry.

# [project]
# name = "foo"
# version = "0.1.0"
# description = "Add your description here"
# readme = "README.md"
# requires-python = ">=3.13.0"
# dependencies = [
#     "baz",
# ]

# [tool.uv]
# override-dependencies = ["baz"]

# [tool.uv.sources]
# baz = { path = ".", editable = true }

# [build-system]
# requires = ["setuptools>=42"]
# build-backend = "setuptools.build_meta"

# Variation: it's pulled from the cache with the wrong name.

[project]
name = "foo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13.0"
dependencies = [
    "baz @ https://files.pythonhosted.org/packages/d7/4b/cbd8e699e64a6f16ca3a8220661b5f83792b3017d0f79807cb8708d33913/iniconfig-2.0.0.tar.gz",
]

# # Variation: this complains about workspace package?

# [project]
# name = "foo"
# version = "0.1.0"
# description = "Add your description here"
# readme = "README.md"
# requires-python = ">=3.13.0"
# dependencies = [
#     "foo",
# ]

# [tool.uv]
# override-dependencies = ["foo"]

# [tool.uv.sources]
# foo = { path = ".", editable = true }
