[project]
name = "foo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anyio>=4.4.0",
    "flask[dotenv]",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
