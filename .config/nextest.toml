[profile.default]
# Mark tests that take longer than 10s as slow
slow-timeout = "10s"

[[profile.default.overrides]]
# The following test has a non-determinstic snapshot
filter = 'test(excluded_only_compatible_version)'
retries = 2
